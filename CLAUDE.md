# pm-cli

Local task tracker. Data: `~/.claude/pm/<project-slug>/`, binary: `pm`.

## Build

```
make install              # build + install (uses VERSION from Makefile)
make check                # gofmt-check + vet + staticcheck + go test ./...
make test-race             # go test -race ./... (also run by CI, not part of check)
make install VERSION=X.Y.Z  # override version
```

Git hooks live in `githooks/` (versioned; `git config core.hooksPath githooks` - already set locally). `pre-commit` just runs `make check`, so the hook, `make check`, and CI share one target set and can't drift apart. `staticcheck` is resolved via PATH → `go env GOBIN` → `GOPATH/bin` and fails with an install hint when missing (`go install honnef.co/go/tools/cmd/staticcheck@latest`). Fix failures - never bypass with `-n`.

CI (`.github/workflows/ci.yml`) runs on push to `main` and on every PR: `make fmt-check`, `make vet`, `make staticcheck`, `make test-race` - the same Makefile targets used locally, plus the race detector (deliberately not part of `make check`, which stays fast for the local commit-time gate).

IMPORTANT: Always use `make install` (not raw `go install`). Version is set via ldflags in Makefile. Bump `VERSION` in Makefile on each release. Current: **0.61.0**. Binary goes to `~/.local/share/go/bin/pm` (GOBIN).

## Tests

```
go test ./internal/... -v          # all tests
go test ./internal/storage/ -v     # single package
go test ./internal/storage/ -run TestSlugify -v  # single test
```

Always add tests when implementing new features or fixing bugs. Conventions:
- Go stdlib `testing`, table-driven tests, `t.Run("case", ...)` subtests
- `t.TempDir()` for I/O tests (auto-cleanup)
- Same package tests (access to unexported functions)
- `setupTestStore(t)` helper in `store_test.go` for tests needing a Store with project + tasks
- Invariant tests (links merge, Log append, Spec rewrite, brief lifecycle) live in `tools_test.go`; the `ApplySpec`/`ExtractSpec` unit tests live in `internal/storage/spec_test.go`
- **Executor paths**: test through a FAKE `claude` binary (`fakeClaude` in `internal/cmd/work_exec_test.go` - a PATH-prepended script emitting a canned result envelope), never by spending tokens; covers verified/blocked/crash/garbage paths of `executeWork` (the envelope's green word was `merged` before 0.34.0 and is still accepted - `normalizeWorkerStatus`)
- **MCP handlers**: test END-TO-END via the SDK in-memory transport (`startMCP` in `internal/mcpserver/e2e_test.go`) so the real registered closures run - do NOT re-simulate handler logic in the test body (the pre-0.23 tests did, which is why coverage sat at 12%)
- **Concurrency**: lock/race invariants live in `internal/storage/lock_test.go` + the `TestAcquireWorktreeLockAtomicRace` two-acquirer hammer in `worktree_test.go`; run suspects under `-race`

## Verification

- After code changes: `make check` (runs fmt-check + vet + staticcheck + test); run `make test-race` too before pushing anything touching concurrency/locking
- After TUI changes: `make install` + run `pm board` and test interactively
- After MCP changes: `make install` + test with `pm_context` / `pm_list_tasks` in Claude Code
- After storage changes: verify task files in `~/.claude/pm/` have correct frontmatter

## Key patterns

- **Frontmatter tasks**: `.md` file with YAML frontmatter (id, title, status, created, updated, status_changed, links, branch, parent, tags, brief, ac, waiting_for, order, sessions, depends_on, mode, model, epic_mode, finish_mode) + markdown body
- **Blocker fields** (`waiting_for` + `status_changed`): `waiting_for` is free text naming who/what a task is blocked on - unvalidated, never required (not even on `waiting`), tri-state over MCP like `brief`/`ac`. `status_changed` is an RFC3339 stamp of the last REAL status change, written by `Task.SetStatus` (`internal/storage/task.go`) - the one place that stamps it ON A MUTATION, so `Store.MoveTask` (every TUI/CLI/executor move) and MCP `pm_update_task` (which writes the task itself) share one rule. SetStatus is a no-op when the status is unchanged: re-stamping a move-to-the-same-status would reset the "stuck since" clock the field exists to report. The other two write sites are the CREATION ones - `NewTask` and the board's add literal (`update_menus.go`), which cannot use NewTask - and both take `updated` and `status_changed` from ONE `Now()` call, so a new task can never carry a status stamp a second later than its edit stamp. Nothing ever clears `waiting_for`: it is leftover text on a task that has moved on, not a live blocker. The board's undo (`doUndo` in `actions.go`) is the one status change that deliberately does NOT re-stamp - it restores the snapshot wholesale, stamp included, which is what undoing a move means. Files are never migrated, so it is EMPTY on older tasks - readers render that as unknown and never fall back to `updated`, which moves on any edit.
- **Task stamps** (`internal/storage/stamp.go`): `created` is a date (`storage.Today()`, `YYYY-MM-DD`); `updated` is a full local-zone RFC3339 timestamp (`storage.Now()`) so tasks touched the same day still order "most recent first". Files are NEVER migrated, so every reader tolerates both shapes: parse with `storage.ParseStamp` (RFC3339 or bare date, local zone), display a human date with `storage.StampDate`. Sorting still compares the raw strings (`LessByOrder`, the board columns, the picker's `lastUpdated` max in `update_picker.go`, MCP `tools.go:318/1053/1142`) and a bare date sorts before the same day's timestamps, which is the accepted trade for not migrating a thousand files - but it is only exact WITHIN one utc offset, so two stamps written either side of a DST switch can invert by up to an hour. Comparators that parse (`SortRunRows`) are exact. MCP returns the RAW `updated`, TUI/CLI display the date - and both display paths (`StampDate`, `relativeTime`) answer "which day" in the READER's local zone. The field stays a `string` so the stamp round-trips byte-exact instead of becoming a yaml `!!timestamp`.
- **Parent/subtask rollup**: `parent` in frontmatter makes a task a child of a tracker (derived: a task is a tracker iff another task names it as parent). `storage.BuildTrackers` generates the rollup used by `pm_context`/`pm context` - never hand-maintain status tables in the parent body. Lifecycle `todo → doing → merged|pushed → done`; children sort by `storage.LessByOrder` (shared with the board columns and the TUI tracker list); set `order` via MCP, `pm add --order`, or `pm reorder`. (history: docs/design-log.md)
- **Brief field**: `brief` in frontmatter stores short session context ("where we left off"). Overwrites on each update (not append). Preserved on all status transitions (including done/archived - useful for summaries/reverts). Returned by `pm_context` for doing tasks.
- **AC field**: `ac` in frontmatter stores acceptance criteria. Overwrites on each update (same as brief). Preserved on all status transitions. Returned in `pm_context` and `pm_list_tasks` summaries. When executing a task, constrain work to AC - don't add unrequested features or deviate from stated criteria.
- **Spec / Log body zones**: two zones, opposite write rules (solves append-only-rot). **Spec** = current truth between `<!-- spec:start/end -->` markers, rewritten wholesale via `storage.ApplySpec(body, spec)` (`internal/storage/spec.go`; `ExtractSpec` reads it); everything outside = append-only **Log** (`body_append`). If markers are absent when `spec` is first set, a block is prepended and the existing body becomes the Log. The TUI renders the markers as visible headers (`bodyToDisplayMarkdown` in `view_detail.go`). Canonical flow: rewrite Spec + append a one-line pointer to the Log.
- **Concurrency (lost-update protection)**: `Store.LockProject(slug)` (internal/storage/lock.go) is a cross-process flock on `<projectDir>/.pm.lock` - hold it around FindTask -> mutate -> WriteTask and re-read the task FRESH inside (wired into the mutating MCP handlers, `pm add`, `pm reorder`, `applyWorkerResult`/`recordSubFeedback`, and `Store.MoveTask`, which self-locks and treats the fresh re-read as a PRECONDITION: a vanished task is an error, no write). Never nest LockProject in one process; partial project.yaml edits go through `Store.MutateProject(slug, fn)`; `LockProject` errors on a missing project dir, never creates it. (history: docs/design-log.md)
- **Identifiers are PATH COMPONENTS, validated at the store**: `storage.ValidateTaskID` in `Store.AddTask` (before the O_EXCL claim), `storage.ValidateSlug` in `Store.CreateProject` (before MkdirAll), `storage.ValidateProjectPrefix` in create/update/mutate (only when the value CHANGES). `ValidateTaskID` is deliberately wider than `ValidateSlug` (digits-only/uppercase legal; spaces/non-ASCII rejected). (history: docs/design-log.md)
- **`pm work`/`pm run-epic` one-arg resolution** (pm-cli-74-1): the one-arg form scans projects in `FindTask`'s three tiers (exact ID -> unique ID prefix -> title substring), each tier deciding alone, ERRORING with candidates on a cross-project tie; cwd is a tiebreak INSIDE a tier, never a short-circuit; archived projects excluded unless you stand in one. (history: docs/design-log.md)
- **Project config**: `~/.claude/pm/<slug>/project.yaml` — name, path, repo, stack, notes, links, tags, statuses, `group`, `archived`. Writes go through `writeProject` (0.29.2): the fresh marshal is MERGED into the existing YAML node tree - comments and unknown top-level keys in a hand-tuned file survive (unchanged subtrees keep their original nodes), cleared known fields drop - and the file is written atomically (tmp+rename). `pm executor init` re-running over an annotated executor block no longer destroys the annotations.
- **Project groups + asleep = `archived`** (pm-cli-118-12): `group:` in project.yaml (a slug, `storage.ValidateGroup`, validated on create and on a CHANGE in `MutateProject` like the prefix) joins several repos into ONE cockpit project (ACME = acme-api + acme-zap + ...); a project with no `group` is a group of its own under its slug (`Project.GroupSlug`). `Store.ProjectGroups` DERIVES the groups from the active projects (no membership list anywhere); `archived: true` is THE asleep flag - an archived project is in no group and no cockpit aggregation, and the board's `hidden_projects` (tui-config.yaml) is UI state that is never read as a fact. The TUI/CLI ignore `group` and `writeProject` preserves it (it is a KNOWN key now). `service.ListProjects`/`pm_list_projects`/`/api/projects` carry `group` + `group_name`; `pm_create_project`/`pm_update_project` take `group` (tri-state on update); `service.ListGroups` = `/api/groups`.
- **Global `config.yaml` `cockpit:` block** (`internal/storage/cockpit.go`): every cockpit setting in one place next to `remotes` - `groups` (slug -> display name), thresholds (`doing_idle_days` 7, `waiting_highlight_days` 5, `stuck_project_days` 14), `cutoff_hour` (18), `refresh` (`every: 30m`, `window: 07:00-20:00`, `RefreshConfig.InWindow(now)` for the scheduler), `sections`/`sources` toggle maps over the CLOSED lists `CockpitSections`/`CockpitSources` (an unknown name is a load ERROR, never a silent no-op), `sidebar` (`variant` columns|plain|rail, `show_repos`, `sort` worst|last_activity|manual, `width`). `LoadConfig` decodes the file OVER `DefaultCockpitConfig()`, so an absent key keeps its default and an explicit zero wins (no pointer fields); no file/no block = defaults, an unparsable or invalid file = a loud error naming the key. This file owns type + defaults + parser + validation only; the MEANING of each field belongs to the consumer (attention aggregation, change feed, SPA). `Store.SaveConfig`/`MutateConfig` are the first writers of config.yaml: merged into the existing node tree (`mergeYAMLDocuments`, the `writeProject` mechanism), comments and unknown keys kept, validated BEFORE the write, the cockpit block written as resolved (defaults become explicit keys on the first save), no lock. `pm config show` prints the block resolved.
- **Statuses are per-project** (`Store.GetProjectStatuses(slug)`; default `[todo, doing, waiting, done]`, override in `project.yaml`; the ALL view merges all projects'). Every mutation path validates: `Store.MoveTask` and the MCP `status` params reject a status outside the project's set (`archived` always legal); `pm run-epic` hard-gates an unlisted `done_status` at run start. (history: docs/design-log.md)
- **Archive is system-level** — `StatusArchived` is NOT a project status. Never add to `DefaultStatuses` or `project.yaml`. `filteredTasks()` excludes archived. `Ctrl+a` toggles archive view.
- **TUI has 7 views** (`view` enum in `internal/tui/board/types.go`): `viewBoard`, `viewDetail`, `viewArchive`, `viewProjectInfo`, `viewFocus` (focus mode, entered in `update.go`), `viewExecutor` (the live agent-view, entered from `executor_view.go`), `viewRuns` (cross-project run list, `R`). `previousView` controls where detail/escape returns to; `viewExecutor` and `viewRuns` use their OWN `executorPrevView`/`runsPrevView` so opening them does not clobber detail's.
- **Project timeline** (`internal/storage/timeline.go`, `pm timeline`): append-only record of what happened TO a project (a task Log is what happened IN a task; the change feed is never imported). Entries `{id, ts, kind, text, refs, session}` with kind exactly `event`|`decision`|`state`; a `state` is a dated snapshot ENTRY (10-20 lines), never an overwritten field, and older states stay. JSONL per month at `<projectDir>/.timeline/<YYYY-MM>.jsonl` (month from the entry's own ts), nothing declared in project.yaml, id derived like `incidentID`, no lock (one O_APPEND line), a ts after today refused, lines read with no length cap. `TimelineDefaultRead(entries, now)` = the latest DATED state + every entry after it (oldest first), `stale` at `TimelineStaleEntries` 10 entries or `TimelineStaleDays` 7 days, no state = newest `TimelineNoStateLimit` 10 + "no state yet"; `FilterTimeline` = newest first, undated last. These types are the contract every surface builds on. CLI: `pm timeline [project] [--json]`, `add --kind --text (- = stdin) --ref --date`, `list --since --kind`. (history: docs/design-log.md)

## TUI patterns

- **`internal/tui/board` file map** (split by concern - grep by name): `update.go` core dispatch + per-view handlers (`update_picker/detail/modes/menus/runs.go`); rendering `view.go`/`view_detail.go`/`view_menus.go`/`view_modes.go`/`view_runs.go`; state `model.go`/`types.go` (the `view` enum + `Model` with its embedded state clusters); `overlay.go` = `overlayLadder`, the ONE ordered table both `View` and `Update` resolve overlays through (add new overlays there, not in per-view if-ladders); `focus.go`, `scroll.go` (cursor/scroll geometry), `actions.go` (status mutations + `snapshotTask`); process launching in `launch_*.go`/`tmux.go`/`worktree.go`/`session.go`/`run_control.go`; `util.go`. (history: docs/design-log.md)
- **`reload()`**: canonical way to refresh state - calls `loadTasks()` + `loadProjectCounts()` + `fixCursors()`. Don't call these separately.
- **Tab counts**: `projectCounts map[string]int` shows non-archived task count per project in tab labels.
- See `.claude/skills/tui-patterns/` for overlay menus, confirmation pattern, toast, navigation details.

## Debugging TUI layout

When fixing layout/rendering bugs, don't guess at line counts - instrument (colored section markers, a debug status bar with `h`/`curH`/`mch`, a `-dbg` version suffix) and have the user screenshot; remove all debug code before committing. Key formula: a view's total visual lines = `strings.Count(output, "\n") + 1` and MUST equal `m.height`, or bubbletea cuts off the top. `maxCardHeight` = `m.height - overhead` (the per-line height budget and instrumentation recipe: docs/design-log.md); column content is clamped to exactly `maxCardHeight` newlines so `JoinHorizontal` stays consistent.

## Gotchas

- **Headless executor workers cannot write anywhere under `~/.claude`** (CC sensitive-path guard; `claude -p` has no one to approve the prompt, so it is a hard refusal, unlike an interactive session which just asks). That is why the source lives in `~/repos/pm-cli` while the data dir stays in `~/.claude/pm/` - and why a worker task that needs to write into the data dir will fail.
- **Headless workers run with `--strict-mcp-config`**: user-scope MCP is invisible to them (a project `.mcp.json` is passed back in); skills unaffected; `userMCP=true` in `buildClaudeArgs` restores it.
- `storage.DefaultStatuses` is the single source of truth for defaults - don't hardcode `[todo, doing, done]` elsewhere
- `ParseStatus` returns `TaskStatus`, not `(TaskStatus, error)` - it lowercases any string
- Task files live next to `project.yaml` in the same dir (not in a subdirectory)
- Links is `map[string]string` (freeform key=url), not a typed struct
- Version is set via ldflags at build time (`internal/version/version.go`) - defaults to "dev" without ldflags
- `cardHeight()` is a reference estimate only - `fixScrollOffsets` and `viewBoard` render cards and measure the actual `\n` count (text wrapping).
- **Task reorder**: `Ctrl+j/k` swaps task order within a column via `Order int` field in TaskMeta. If all tasks have `Order==0`, sequential orders are assigned first. `filteredTasks()` sorts by Order asc, then Updated desc.
- **Sessions**: `Sessions []string` in TaskMeta stores Claude session IDs (appended by TUI on launch, or via MCP `sessions` param). Used for `--resume` and `session_count` in MCP output. MCP `pm_get_task` returns full `sessions` list; `pm_update_task` and `pm_add_task` accept `sessions` param (append-only).
- **Session detection**: `pm session-id` prints current CC session UUID. Strategy: (1) walk process tree for `claude --resume <id>` arg, (2) fallback to newest .jsonl in `~/.claude/projects/<encoded-cwd>/`. Requires `CLAUDECODE=1` env var.
- **Worktree naming**: `worktreeName(t)` uses `t.Meta.Branch` if set, otherwise `Slugify(t.Meta.Title)`. Produces semantic branch names (e.g. `feat/enable-analytics` not `atlas-9`).

## Workflow

- Branch: substantive work (features, fixes, refactors, review-fix batches) goes on a feature branch and merges via PR, per repo history (`git log --merges`). Small standalone tweaks may still land as a direct commit on `main`.
- Commit messages: imperative, concise, prefix with feat/fix/refactor
- Always bump version in build command when releasing

## Agent docs (`pm docs`)

The canonical agent-facing usage contract lives in `docs/agent-guide.md` (wrapped in `pm:agent-guide` markers) + `docs/task-authoring.md`, embedded via go:embed and printed by `pm docs claude` / `pm docs authoring`; a user's CLAUDE.md is a CONSUMER of that output, never a second copy to hand-sync. **When tool semantics or the usage contract change, update `docs/agent-guide.md` in the same PR.** (history: docs/design-log.md)

## MCP server (`pm mcp`)

Stdio MCP server for Claude Code. Registered as user-scope MCP: `claude mcp add --transport stdio --scope user pm -- ~/.local/share/go/bin/pm mcp`.

- **Package**: `internal/mcpserver/` — `server.go` (setup + Run), `tools.go` (10 tools), `journal.go` (`registerJournalTools`, 2 tools), `timeline.go` (`registerTimelineTools`, 2 tools) = **14 registered tools**
- **Every tool's semantics live in `internal/service`; the 14 MCP handlers are thin** (pm-cli-118-3/-4): `service.AddTask/UpdateTask/MoveTask/DeleteTask` (mutations: status validation, tri-state, link merge, Spec/Log, `LockProject` with the FRESH re-read, the load-bearing mode/epic_mode/finish_mode pre-checks), `ListTasks/GetTask/Context` (+`ProjectContext`/`CrossProjectContext`), `ListProjects/CreateProject/UpdateProject` (slug/prefix validation, `validateStatusesKeepTasks`), `JournalAdd/JournalList`, `TimelineAdd/TimelineList` - all over `storage.TaskStore`. The handlers only decode -> call -> `respond` (`jsonText`/`toolError`), and the coming `pm serve` HTTP API calls the same functions. DTOs are exported service types in `internal/service/dto.go` (`TaskSummary`, `TaskDetail`, `ListTasksResult`, `ProjectContextResult`, `CrossProjectContextResult`, `ListProjectsResult`, `ProjectResult`, `JournalEntriesOutput`, `JournalAddResult`) with the json tags that ARE the agent contract; where a shape used to be a `map[string]any`, the struct's field order is the map's alphabetical key order so the wire bytes did not change (diffed before/after on a fixed store, schemas included). The argument structs (`service.AddTaskInput` etc., `jsonschema` tags included) are the ONE copy of every parameter description - `tools.go`/`journal.go` alias them, so the generated tool schemas did not change. Output budgets (`DefaultListLimit`/`MaxListLimit`/`ContextBodyLimit`, `DefaultJournalLimit`/`MaxJournalLimit`) are service constants. Caller-caused failures are `*service.ValidationError` (unwrap to the underlying error); the package must never import the MCP SDK. Unit tests in `internal/service/*_test.go`; the e2e MCP tests remain the behaviour proof. (history: docs/design-log.md)
- **Subcommand**: `internal/cmd/mcp.go` — `pm mcp` cobra command
- **SDK**: `github.com/modelcontextprotocol/go-sdk/mcp` — typed `AddTool[In, Out]` for auto schema generation
- **Tools**: `pm_context`, `pm_list_tasks`, `pm_get_task`, `pm_list_projects`, `pm_add_task`, `pm_update_task`, `pm_update_project`, `pm_create_project`, `pm_move_task`, `pm_delete_task` (all in `tools.go`), plus `pm_journal_list`, `pm_journal_add` (in `journal.go`, added 0.35.0), plus `pm_timeline_list`, `pm_timeline_add` (in `timeline.go`)
- **Timeline over MCP** (`internal/service/timeline.go`): `TimelineAdd` (kind/text checked as `*ValidationError` before the append; answers the entry + `stale`/`entries_since`/`days_since` + a note), `TimelineList` (no since/kind/limit = the default read `TimelineReadResult` = `project` + the flattened `storage.TimelineRead`; any of them = `TimelineEntriesResult{entries, total = matching, shown, note}` newest first, `DefaultTimelineLimit` 20 / `MaxTimelineLimit` 200 clamped), `TimelineDefaultRead(store, project)` = the shared default read (ErrProjectNotFound for an unknown project). Project-scoped `pm_context` carries `timeline` (`TimelineContext`: the state WHOLE, the newest `ContextTimelineLimit` 20 entries after it + `since_omitted`, stale, note) only when the project has an entry; the cross-project rollup carries `timeline_state` ("YYYY-MM-DD: first line", " (stale)") only when a state exists. The tool descriptions carry the agent rules (task Log vs timeline, a state fits on a screen, write at the brief checkpoint).
- **No MCP resources** (pm-cli-72-3): removed - each duplicated a tool with a worse copy and bypassed the output budgets. Use `pm_list_projects`, `pm_list_tasks`, `pm_get_task`/`pm_context`. (history: docs/design-log.md)
- **CWD auto-detection**: `pm_context` with `cwd` param matches against project.yaml `path` fields
- **Output budget** (0.24.0): `pm_list_tasks` returns the 50 newest tasks (`limit` overrides) as `{tasks, total, shown, note}` with ONE-LINE briefs (`storage.BriefLine`; `ac` stays full); `pm_context` caps each doing-task `body` at 2000 runes but never truncates `brief`; `pm_get_task` is unbounded. Tracker briefs are one line (`brief_line`) and a finished tracker collapses its children (`children_omitted: true`); the project-scoped `doing_tasks` brief stays FULL on purpose. (history: docs/design-log.md)
- **Attention aggregation** (pm-cli-118-13, `internal/storage/attention.go`): ONE cross-project queue of what needs the human, computed in storage (`BuildAttention(store, cfg, opts)`) and consumed by three surfaces off the same value - `/api/attention` (`service.Attention`, `?project=`/`?group=` via `Attention.Scope`), `pm today` (`--json` = the API shape; `internal/cmd/today.go`), and the `attention` block of `pm_context` (`service.DigestAttention`: needs_me + stuck_projects in full, waiting capped at `ContextWaitingLimit` 10 with `no_reason` rows promoted, every other enabled section as a count; best-effort - a config error is a note, never a failed context). Inputs are LOCAL FILES ONLY (tasks of non-archived projects, `.executor/` run/finish states + claims, focus.yaml, `group`, the `cockpit:` block); the `changes` section only EMBEDS a `ChangesDigest` the caller hands in (118-14), never fetches. Sections in `CockpitSections` order, each on/off from `cockpit.sections`, a disabled one is NOT emitted. **A tracker the human closed (`done` / `archived`) is skipped in `needs_me` and its children in `landed_no_pr`** (`closedByHuman`; landing statuses do not count - they are the executor's): the run-state files are the machine's memory, the status is the decision. `needs_me` ranks (a) run failed / crashed (= run-state `running` with a dead pid) › (b) acceptance failed/crashed › (c) open visual claims › (d) run done with no acceptance and no claim › (e) acceptance partial › (f) live claim (checked BEFORE the acceptance state, the acceptCell rule), oldest first within a rank. a waiting row also carries the raw `waiting_for` (the value the row's reason dialog pre-fills - the reason string is prose, not a field); `waiting` ages come from `status_changed` ONLY (empty = `age_seconds: null`, "since ?" - never `updated`); `stuck_projects` uses activity = max(updated, status_changed) on purpose (rule 1: no task change for `stuck_project_days`; rule 2: >=1 doing task, every doing task idle `doing_idle_days`, nothing on focus). `focus` carries an older plan over (`Cleanup` semantics against the INJECTED clock, flag `stale_plan`). Rows share one shape with a closed `actions` set decided by the row's state (`ActionOpen`..`ActionOpenPR`) - a UI renders, never invents; `GroupSummary` = worst severity + failed/visual/waiting/quiet counters + last_activity, sorted worst-first. `wip` = doing tasks with activity since Monday. `recent` is configurable but NOT computed (the session-file lookup lives in the TUI); its section says so in `note`. Tests: `attention_test.go` fixture covers every AC case with a fixed clock (`AttentionOptions.Now`).
- **Trackers rollup**: `pm_context` returns a `trackers` section (parent+subtask rollup) when any task has children. Children/trackers are suppressed from the flat `doing_tasks` list. `pm_add_task`/`pm_update_task` accept a `parent` param. Same rollup is available offline via the `pm context [project]` CLI command (`internal/cmd/context.go`) - useful when the running MCP holds a stale binary.
- **Executor fields over MCP** (0.26.0; `finish_mode` 0.41.0; `runtime` pm-cli-122): `pm_get_task` returns `model`, `epic_mode`, `finish_mode` and `runtime`; `pm_add_task`/`pm_update_task` accept `epic_mode`, `finish_mode` and `runtime` (update: omit = keep, `""` = clear - the `mode`/`model` pattern). Validation is enforced in `writeTask` (`storage.ValidateEpicMode`/`ValidateFinishMode`/`ValidateRuntime`, like `ValidateMode`); the handler-level pre-checks are LOAD-BEARING, not cosmetic - a writeTask failure after the O_EXCL claim would leave a 0-byte phantom file (pm-cli-41).
- **Invariants**: links merge (never remove); `body_append` appends to the Log; `spec` rewrites the Spec block in place; tags replace if provided. `branch`/`parent`/`brief`/`ac`/`waiting_for` are tri-state on `pm_update_task` (omit = keep, `""` = clear); `title` is never clearable. `status_changed` is never a param - pm stamps it itself on a real status change. `status` params reject values outside the project's set (`archived` always legal); `pm_update_project` rejects a `statuses` replacement that would orphan tasks (an empty list resets to defaults and is checked against THAT set); `pm_create_project` validates `slug` via `storage.ValidateSlug`. (history: docs/design-log.md)

## Web (`pm serve`) — HTTP face of the cockpit (reads + mutations + run control + the feed's sources)

`pm serve --addr 127.0.0.1:7070` (`internal/cmd/serve.go`) serves `internal/server` (pm-cli-118-5): stdlib `net/http` with Go 1.22 method+pattern routing, NO framework, no auth, no TLS (localhost; remote access = Tailscale). (history: docs/design-log.md)

- **`pm serve` is also an MCP CLIENT** (pm-cli-118-19, `internal/feed/mcp_client.go`): the feed's `slack` source runs the user's registered Slack MCP server as a stdio subprocess (go-sdk `CommandTransport`) and calls its tools itself - no LLM in the loop - so Slack reaches the cockpit through the one place the tokens already live (`~/.claude.json`) instead of a second Slack client with tokens of its own.

- **Package map**: `server.go` = `NewHandler(store, Options)` + the JSON endpoints + the SPA handler; `events.go` = the SSE feed; `dist/` = the go:embed'd bundle (only `.gitkeep` is versioned - `make web` fills it, `.gitignore` keeps it out). `Options` = `Static fs.FS` (tests pass an `fstest.MapFS`), `RemoteRuns` hook, `PollInterval`/`PingInterval` (defaults 2 s / 15 s).
- **Reads, and the DTOs are the service's**: `/api/projects` (ListProjects rows + path/repo/tags/links/statuses/landing_statuses; `group`/`group_name`/`archived` ride on the ProjectInfo), `/api/groups` -> `service.ListGroups` (loud 500 on a broken config.yaml, where `/api/projects` only notes it), `/api/attention[?project=|?group=]` -> `service.Attention` (the whole `storage.Attention`, its `changes` section embedding the feed cache's digest; 500 on a broken config - the thresholds ARE the queue's definition), `/api/changes` + the two POSTs (below), `/api/tasks` -> `service.ListTasks`, `/api/tasks/{project}/{id}` -> `service.GetTask`, `/api/context` -> `service.Context`, `/api/runs` -> `storage.LocalRunRows` (+ the injected remote fetch ONLY on `?remote=1` - an ssh round-trip per runner is never the default a dashboard polls; since pm-cli-118-17 a row also carries the RAW run-state stamps `run_started`/`run_updated`/`run_live` and `accept_started`/`accept_updated`, so a reader can show duration, time since the end and the heartbeat age without a second call - `pm runs --json` gains the same fields, the CLI table ignores them), `/api/focus` -> `storage.ReadFocusPlan` + `service.FocusTaskSummaries`, `/api/config` -> `service.Config` (pm-cli-118-15: the RESOLVED `cockpit:` block - defaults applied server-side, durations as `every_seconds`, `groups` as a slug-sorted array because a JSON object carries no order; loud 500 on a broken file, the ListGroups rule - the SPA shapes its sidebar and sections from it and must never know the defaults; the settings screen, 118-18, adds the write). Never define a second copy of a DTO here. Every route is `GET`-qualified so the mux answers a POST with 405 itself.
- **Errors** are `{"error": "..."}`: `*service.ValidationError` -> 400, `storage.ErrTaskNotFound`/`ErrProjectNotFound` -> 404 (sentinels wrapped into FindTask/FindTaskExact/ResolveProject with the messages unchanged), `storage.ErrAmbiguousTask` -> 400, anything else 500; an unknown `/api/*` path is a JSON 404, never the SPA.
- **Change feed (`internal/feed`, pm-cli-118-14)** - "what changed since yesterday 18:00" from SOURCES behind one interface (`feed.Source`: `Name()` + `Fetch(ctx, from, to, projects) ([]Event, error)`; a per-project failure is a `*ProjectError` joined with the other projects' events); the registry is `feed.Sources(root, runner)` = `pm` (status_changed, created on/after the cutoff's DATE, executor journal start/end/killed/crashed, acceptance run-states, the focus plan - zero network), `git` (commits on every branch a task names via `git log --since/--until` in the project's `path` - a branch that does not exist locally is skipped, `AllBranches` widens it to `--all`; PRs via `gh pr list --state all --search updated:>=<cutoff date - 1 day>` so a merge or a close is an event of its own - `PR #N by X merged by Y` (severity ok) / `closed without merging`, emitted INSTEAD of "updated" - and every listed PR lands in the PR snapshot, below), `github`/`git` PR calls go through `githubContext`: a checkout whose `git remote -v` names no `github.com` (GitLab, no remote) is skipped WITHOUT an error, and a project.yaml `gh_account` puts `gh auth token --user <account>` on the context as `GH_TOKEN` (`feed.WithEnv`/`EnvFrom`, appended by both runners - never argv) because `pm serve` never loads a repo's direnv token; `github` (comments + reviews + decision via `gh pr view --json` for the PRs touched in the window only, merged/closed ones included - talk after a merge is still news; machine chatter is dropped by `noiseComment`: `noiseAuthors` (github-actions, linear-code, vercel, dependabot, codecov...), a `<!-- linear-linkback -->` body, a one-line slash command like `/preview` - a review bot such as `claude[bot]` is deliberately NOT noise). **PR snapshot**: a source implementing `feed.PRSnapshotter` (the git source) hands `Refresh` the current `PRState` of every PR it listed; `mergePRStates` writes them over `<cache>/prs.json` PR by PR (a PR missing from this listing keeps its row, rows idle 14 days drop) and `Feed.PRStates(since)` reads it - events are history, this is where each PR stands now. Both PR paths share one filter in `listPRs` (pm-cli-135): a PR is kept only when the user wrote it, a review is requested from the user, or its head is a pm task's `branch` - the user's login is `gh_account` when set, else `gh api user --jq .login` once per project (a failure is that project's error), compared case-insensitively. A new source = one file + one registry line. `Event.ID` is STABLE (`feed.EventID(parts...)`), which is what makes a refresh idempotent: the cache under `<pm-root>/.cockpit/changes/` (JSONL per event day, the journal's grain; `state.json` = source statuses + the `seen` stamp; 14-day rotation) dedups on it and holds ONLY in-window events. `Feed.Refresh` runs every ENABLED source (`cockpit.sources`), records each one's `SourceStatus{last_fetch, error, events}` and NEVER lets one source's failure stop the others; `Feed.Read(cutoff)` applies the seen flag from the cache alone; `Feed.Digest`/`feed.ReadDigest` shape the cache for the attention queue's `changes` section (unseen count + newest unseen rows) - the aggregation never fetches. External commands go through a `feed.Runner` (`DefaultRunner`: own process group, `feed.CommandTimeout` 30 s, group SIGKILL on expiry, pagers/prompts disabled; `pm serve` injects `feedRunner` built on `groupCmd`); tests fake `gh` and run real `git` on a temp repo. The cutoff is `feed.Cutoff(cfg, now)` = `storage.CutoffSince` (before 18:00 -> yesterday 18:00, else today 18:00).
- **Feed endpoints + scheduler** (`internal/server/changes.go`): `GET /api/changes` (cache: `{cutoff, events[] with seen, unseen, seen, sources[]}`), `POST /api/changes/refresh` (runs the sources NOW whatever the window says, 5-min outer wall, answers the `feed.Result`), `POST /api/changes/seen` (`{ts}` optional, default now); each of the two POSTs publishes the SSE `event: changes` (`data: {"sources":[...]}`) through a `changeBus` fan-out every open `/api/events` stream subscribes to (non-blocking send; a missed signal is caught by the next). `NewHandler` now returns `*server.Handler` (an http.Handler) with `RunScheduler(ctx)`: once AT START (a restart must not leave the cache stale for a whole interval) and then every `cockpit.refresh.every` (re-read per tick; 0 = off) it calls `schedulerTick`, which refreshes ONLY when `RefreshConfig.InWindow(clock())` - outside 07:00-20:00 nothing reaches git/gh unasked. `Options.Clock` injects the clock (the window test runs at 12:30 and 22:30 without waiting), `Options.Feed` the feed (tests hand in fake sources). A GET on a POST-only route is the `/api/` catch-all's JSON 404, not a 405.
- **SSE says WHAT changed, never what to** (`event: tasks|runs`, `data: {"project": slug}`; `event: changes`, `data: {"sources": [...]}` after a feed refresh or a seen mark; `event: ping` while idle): a fixed-interval poll fingerprints each active project from directory METADATA only (names/sizes/mtimes of `*.md` + `project.yaml`, and of `.executor/*.json` + `*.claim`) - no file content, no fsnotify (zero new deps, and the board already lives on a 2 s tick). The handler returns on `r.Context().Done()`, which is what lets `Shutdown` finish. A heartbeat re-stamping a live run-state is a `runs` event every 30 s per live run - by design, the client refetches `/api/runs`.
- **SPA**: `//go:embed all:dist` - the `all:` prefix is load-bearing (without it a dot-file-only dir embeds nothing and the build fails). A path naming a file is served (`assets/*` = `immutable` cache, Vite hashes the names; anything else `no-cache`); everything else gets `index.html` with `no-store`; no `index.html` at all = a built-in placeholder page saying "run `make web`", with 200 (a 404 would read as "server down").
- **The remote-runs ssh fetch stays in `internal/cmd`** (`remoteRunRows`, which needs `groupCmd` and the test-patched `remoteRunsTimeout`) and is injected via `Options.RemoteRuns` - `internal/server` must never import `internal/cmd` (cycle), and `pm runs` output is untouched.
- **Tests** (`internal/server/server_test.go`): `httptest` over the same temp-store fixture the MCP e2e uses; every endpoint, the error codes, `?remote=1` through a fake hook (never ssh), the placeholder/bundle/fallback/cache-header matrix, and the SSE feed end to end (ping, a task edit -> `tasks`, a run-state file -> `runs`, cancel -> handler returns and `Close` does not hang).
- **The SPA lives in `web/`** (pm-cli-118-6): npm (`package-lock.json` versioned), Vite + React + TypeScript `strict`, Tailwind v4 (utility classes on native HTML elements only - no component library, no design system until the visual pass, pm-cli-118-10), `@tanstack/react-query` + `@tanstack/react-router` (code-based routes in `src/routes/router.tsx` - three routes do not justify the file-based plugin), oxlint + prettier (the current Vite template ships oxlint, not eslint), vitest + testing-library (jsdom). Vite builds into `internal/server/dist` (`emptyOutDir` wipes the `.gitkeep`; `make web` touches it back) and the dev server proxies `/api` to `127.0.0.1:7070`.
- **Make targets**: `web-install` (`npm ci`), `web-check` (lint + typecheck + vitest, what CI's `web` job runs), `web` (bundle), `install-full` (`web` + `install`). `check` and `install` are NODE-FREE on purpose - the pre-commit hook must stay fast and `go build` works with an empty `dist/`.
- **Layer boundary - what makes the UI replaceable, enforce it in review**: `web/src/api/` (`types.ts` = hand-written mirrors of the Go DTOs' json tags, `client.ts` = the fetch wrapper + `ApiError{status}`, `queries.ts` = react-query hooks with the KEYS `['projects']`, `['tasks', slug]`, `['task', slug, id]`, `['context', slug]`, `['runs']`, `['focus']`) and `web/src/lib/` (pure functions, no React, each with a vitest test) are PERMANENT. `web/src/routes/` (composition: hooks in, components out) and `web/src/components/` (props in, JSX out - no `fetch`, no `useQuery`, no grouping/sorting/counting) are REPLACEABLE: the visual pass swaps those two dirs and touches nothing else. A DTO field added in Go is added to `types.ts` by hand - there is no generator, and a drifted field shows up as `undefined` in exactly one place. `web/src/hooks/` (pm-cli-118-9) is React glue with no domain rule of its own - `useShortcuts` binds the window keydown to the map in `lib/shortcuts` (and lets a modal `<dialog>` own the keyboard, palette chord excepted), `useRecentTasks` is localStorage around `lib/recent` - and counts as replaceable.
- **Today + the group sidebar (pm-cli-118-15)**: `/` is the Today screen over `/api/attention` (`routes/TodayPage.tsx`), `?g=<group>` narrows every section to one group through the API's own `?group=` (the filter lives in the URL so a phone bookmark carries it; `indexRoute.validateSearch` + `lib/groupFilter`); the old project list is no longer `/`, its content lives on under `/p/$slug`. Sections render EXACTLY the ones the API returns, in its order - the UI knows their wording (`lib/attentionView` `sectionMeta`: title, one "how it is counted" line, the empty-state sentence) and a view cap (`SECTION_CAP` 7 + "show all", `capSection`; the API's own truncation, e.g. the changes digest, is reported as "+N more on this section's screen"), never their membership. A row (`components/AttentionRow`) is glyph + project chip + id/title + the API's `reason` + age (`lib/ageLabel`: `age_seconds` null renders "since ?", never another stamp) + one button per entry of the row's `actions` - `lib/rowActions` words each action and says which sub enables it (`open` works, `open_pr`/`report` wait for a link/endpoint, the rest are disabled with the sub id as the title until 118-16/118-21 land). Glyphs are text, never colour alone (`lib/glyphs`: severity ✗▲●○, the waiting/stuck sections override with ⧗/◔; the sidebar's counter columns ✗👁⧗◔). The sidebar (`components/GroupSidebar`) reads the `groups` summary of the unscoped attention query and the `cockpit.sidebar` block of `/api/config` through `lib/sidebarView`: `columns` (default: glyph, name, four counters with zeros as dots, member repos under a multi-repo group when `show_repos`), `plain` (no counters), `rail` (glyphs only, names in tooltips), `width` in px; `sort` `worst` = the API's order, `last_activity` sorted in lib, `manual` FALLS BACK to the API order (JSON object keys carry no order - an explicit order field on the config endpoint is what would enable it); asleep projects are one "asleep (N)" line. A group link opens its first repo's board until the group page (118-17) exists (`groupTarget`). On a phone the sidebar is a chip strip over the content (the drawer is gone). Keys: `1/2/3/,` switch screens (`/`, `/changes`, `/runs`, `/settings` - the two 118-18 screens are placeholder routes so the keys already lead somewhere), `j/k/Enter` walk the home rows, `t` is registered for focus (wired in 118-16), `g <letter>` is a chord filtering by group (`groupHotkeys` assigns one letter per group, `useShortcuts` arms it for 1.5 s). New query key: `['config']`. Tests: `lib/*.test.ts` per rule, `shell.test.tsx` home over a fake `/api/attention` (order, empty sentence, cap/elsewhere, actions, `?g=` narrowing via the exact-URL fake route, the chord, `1/3`, `j/k/Enter`, the three sidebar variants + `last_activity`).
- **Mutations batch 1 (pm-cli-118-16) - every POST is the MCP tool's service function, behind one header**: `POST /api/tasks/{project}/{id}` decodes its body STRAIGHT into `service.UpdateTaskInput` (`DisallowUnknownFields` - a typo'd field is a 400, never a silent no-op; the path's project/id override the body) and calls `service.UpdateTask`, so tri-state (`""` clears, omitted keeps), link merge, Spec/Log, status validation and `LockProject` are the service's and never re-stated; `POST /api/focus/toggle` `{task_id}` -> `service.ToggleFocus` (new: the board's `t` over the same `storage.FocusPlan` - the task must exist in an active project, a stale plan is carried over like the board's `handleStalePlan` before the toggle, no lock because focus.yaml is root-level and the board writes it lock-free too); `POST /api/projects/{slug}` -> `service.UpdateProject` (the cockpit sends `notes`; an empty `notes` means KEEP, as in MCP, so notes cannot be cleared from the web). **Every POST under `/api/` needs `X-PM-Client: cockpit`** (`server.ClientHeader`/`ClientValue`, `requireClient`) or it is a 403 before the body is read - the 118-14 POSTs (`/changes/refresh`, `/changes/seen`) included; not auth (there is none, localhost/Tailscale), the one thing a cross-site form or a stray curl does not send. Errors as before (400/403/404/500). Tests: `TestMutations` in `server_test.go` (403 matrix, tri-state, Log append, 400s, 404s, path-over-body, focus round-trip through `ReadFocusPlan` + `/api/focus`, project notes, and an HTTP-vs-service comparison on two identical fixtures whose resulting tasks must match byte for byte minus the stamps); `service/focus_test.go`. **Web**: `apiPost` always sends the header; `api/mutations.ts` = `MutationRequest` (task | focus | seen | project) + `runMutation` + `useRowMutation` (invalidates `['attention']`, `['tasks', slug]`, `['task', slug]`, `['context', slug]`, `['focus']`, and `['changes']`/`['projects']` per kind - explicitly, never relying on the SSE event alone). **Every action goes through ONE confirmation dialog** (the user's rule: nothing fires by accident): `lib/confirmText` is the pure part - `describeAction` (heading = id + title, project, one sentence of what will happen, an optional field pre-filled by `initialValue`, a warning: `waiting` without a reason warns "no reason = alarm", an empty brief warns "clears") and `requestFor` (the exact body: `set_waiting_for` = status waiting + reason in one request, `back_to_todo` = status todo + `waiting_for: ""`, `mark_seen` = the row's `since` as `ts`); `components/ConfirmDialog` renders it in a native `<dialog>` (form submit = Enter in an input, ⌘/Ctrl+Enter in the textarea, Esc cancels; focus moves into the dialog on open so both work); `hooks/useConfirmedAction` is the glue (pending action + value + the mutation + a 3 s toast). Home: the row buttons `focus`/`waiting…`/`back to todo`/`seen` are live (`lib/rowActions` `pending: ''`), `t` on the selected row asks the same dialog, run-control actions stay disabled until 118-21. Detail (`/p/$slug/t/$id`): a status `<select>` over the project's statuses (choosing one opens the dialog; `waiting` asks for the reason), `set/edit reason`, `write/edit brief` (textarea, overwrite like MCP); body/Spec are NOT editable from the web in v1. Tests: `lib/confirmText.test.ts`, `shell.test.tsx` "mutations go through the dialog" (open + Esc = no request, `t` + confirm posts `/api/focus/toggle` with the header, waiting warning + Enter posts status + reason, brief textarea + ⌘Enter, status select, a 400 keeps the dialog open with the error).
- **The group page + the Runs screen (pm-cli-118-17)**: `/g/$group` (`routes/GroupPage.tsx`) is the project page of the cockpit - a group is one client/product over one or more repos (`group` on every `/api/projects` row; `lib/groupView` derives members, name, the board repo) - with the tab in the URL (`?tab=overview|board|runs|changes`, `?repo=` for the board; `[`/`]` step the tabs). **`/p/$slug` is an ALIAS**: with no task open it redirects (`<Navigate replace>`) to that repo's group, Board tab, so old links and the palette keep working; with a task open it stays the detail's layout (board beside the detail) - the board itself moved to `routes/BoardPane.tsx`, shared by both. Overview: "Where we left off" = the `notes` of EVERY member repo (`/api/projects` rows now carry `notes`; editing goes through the 118-16 dialog as `edit_notes` -> `POST /api/projects/{slug}`; the spec's `cockpit.groups.<slug>.notes` does not exist - 118-12 never added it - so per-repo notes are the v1, recorded as a SPEC-CONFLICT in the sub's brief); "Needs me" and "Waiting on" are the SAME `AttentionSection` rows as the home, narrowed by the API's `?group=` (side by side from the `wide:` breakpoint, 820 px, defined in `index.css`); "In progress (doing)" = the doing tasks of every member (one `tasksQuery` per repo via `useQueries`), ordered in `lib/doingView` by activity = the later of `updated`/`status_changed` (the API has no such field; the derivation lives in lib with a test, never in a component), idle past `cockpit.doing_idle_days` (from `/api/config`) marked ◔, a tracker row shows its rollup progress; "Trackers" = each member's `/api/context` rollup joined with its `/api/runs` row (`trackerRows`), cells worded by `lib/runCells`. Board tab = `BoardPane` with a repo switcher; Runs tab = the Runs screen filtered to the members; Changes tab = the feed's events of the group (`filterChanges`, plain list). **Runs** (`routes/RunsPage.tsx`, `lib/runsView`): the default order is "needs me first" = a JOIN with the `needs_me` section of the unscoped `/api/attention` - its order IS the rank (failed › visual › landed without acceptance › partial › live claim), the UI adds no ranking of its own; `newest` = the API's order, `by project` = a stable sort over it; the default filter "unfinished only" = the run is live OR the queue has a row for it (remote and placeholder rows are always kept - they were asked for); the first column is a fixed-width glyph (the queue's severity, ▶ live, ○ otherwise), and three time columns come off the new raw stamps (`runTimes`: duration to the end or to now, since-end for a finished run, heartbeat age for a live one). Tests: `lib/runsView|doingView|groupView.test.ts`, `shell.test.tsx` "group page" (header, chips, tabs, notes, narrowed queue, doing order + idle, trackers join, the `/p/` redirect, repo switch, `[`/`]`, changes tab) and "runs" (default filter + order, the three times, sort toggles, remote fetch).
- **The Changes + Settings screens and the settings write (pm-cli-118-18)**: `/changes` (`routes/ChangesPage.tsx`) is the whole feed off `/api/changes` with the source chips doubling as filter AND status (`lib/changesView` `sourceChips`: off / never fetched / error text / last HH:MM; an erroring source lists its error under the strip), a group filter in the URL (`?g=`, the home's `groupFilter` + the `g <letter>` chord), "refresh now", "mark all seen" and a per-row "seen" (both through the 118-16 dialog as `mark_seen`), unseen rows highlighted, row times as `eventTimeText` (HH:MM today, "yest. HH:MM", else "Mon DD HH:MM"), and the `ReportPanel` placeholder above the feed ("report is off" until 118-20). `/settings` (`routes/SettingsPage.tsx`) is ONE form over the resolved `/api/config` (`lib/settingsView`: `formFromConfig` -> the user edits -> `patchFromForm(base, edited)` = the PATCH of what changed, nothing else, or null) in the wireframe's groups (sources + `git: scan every branch`, cutoff, refresh, thresholds, the read-only needs-me order, home sections in `SECTION_ORDER`, sidebar, project groups with name + manual `order` + a per-repo group select), saved by an explicit Save to **`POST /api/settings`** -> `service.UpdateSettings` (pointer fields = absent keeps; toggle maps and `groups` merge key by key; validated as a `*ValidationError` NAMING the field before the write - hour 0-23, window `HH:MM-HH:MM` ending after it starts, `every_seconds` 0 = off or >= `MinRefreshEvery` 5 min, thresholds >= 1, variant/sort/section/source names from the closed lists, group slugs; then `storage.MutateConfig` keeps comments and unknown keys, and the resolved block is answered). Below the form, the per-project settings write each `project.yaml` through the existing `POST /api/projects/{slug}` (`service.UpdateProjectInput` gained `slack`; `group` and `archived` already decoded): a Slack row (workspace label + channels text, `slackMappingOf` parses "a, #b c"; an empty mapping REMOVES the key), the Asleep section (`archived`), and the repo-to-group select - **sleep, wake and move go through the dialog** (`confirmText` kinds `sleep_project`/`wake_project`/`move_repo`; sleep warns that the project leaves every group and the queue), the form's Save and a Slack row's save are explicit buttons and post directly. **Config on the wire, 118-18 additions**: `groups` is an ARRAY in the manual order (`storage.CockpitConfig.GroupOrder`: placed groups by `order` then slug, unplaced after by slug - a JSON object carries no order, which is why the sidebar's `manual` sort was a fallback until now; `lib/settingsView` `manualSidebarOrder` applies it, groups the config does not name follow in the API's order), each with `order` (`cockpit.groups.<slug>.order`, 0 = unplaced); `git.all_branches` (`cockpit.git`, read by the git source through **`feed.Configurable`** - `Refresh` hands every source implementing `Configure(cfg)` the CURRENT block before its Fetch, so an edit takes effect on the next refresh, no restart; the slack source of 118-19 takes its server list the same way). After a settings write the server publishes **SSE `event: settings`** (`{}`; the `changeBus` now carries `{event, data}`), and the client invalidates `['config']`, `['attention']`, `['projects']`, `['groups']` (`useLiveInvalidation`); the `settings` mutation kind invalidates the same plus `['changes']` explicitly. Nothing is cached per process: every handler re-reads config.yaml, so a change is live on the next request. `pm config show` prints the group order and the git switch. Tests: `service/config_test.go` `TestUpdateSettings` (patch keeps the rest + comments, every bad value names its field and writes nothing, 0 = off), `server_test.go` `TestSettings` (403/400, the write + SSE + next GET, project group/archived/slack with `# comment` kept, empty slack removes the key), `storage/cockpit_test.go` (GroupOrder, negative order, git round-trip, slack in project.yaml), `web/src/lib/changesView|settingsView.test.ts`, `shell.test.tsx` "changes screen" (chips + states, source and group narrowing, mark seen through the dialog with the row's `ts`) and "settings screen" (values from the API, Save disabled until dirty then posts only the diff, sleep asks and warns, a Slack row saves, a repo move asks). Known: `SaveConfig`/`writeProject` re-indent the file to 4 spaces and drop a blank line before a comment on the first write - cosmetic, the 0.29.2 merge's own encoder.
- **Run control from the web (pm-cli-118-21) - `internal/runctl` + `POST /api/runs/{project}/{id}/{action}`**: the attention row's run actions (`claim`, `release_claim`, `rerun_finish`, `resume_run`, `kill`) are the FIRST place HTTP starts processes, and the rules are the board's, re-stated ONCE in `internal/runctl` (no bubbletea; `internal/server` cannot import `internal/cmd`, so the spawn is a detached `pm <args>` like the board's "bg"/"finish" branch, never a call into the manager). `Controller.Plan(project, id, action, flags)` is the one place the argv is decided - it is what `GET /api/runs/{p}/{id}/plan?action=&yolo=1&additional=1` answers (argv, cwd, log, warnings, the claim session, the kill target/pid) and exactly what `Spawn` runs, so the dialog's preview IS the command: `resume_run` = `run-epic <slug> <id>` for a tracker / `work` for a leaf (+`--yolo`, +`--additional` only when the project has worktree slots - `additional_avail`), `rerun_finish` = `finish <id> --project <slug> --no-sim` (+`--additional`) and **never `--sim`** (nobody may be at the screen; there is no tmux variant here). Warnings are the board's (a live run of the same kind, not a git checkout / dirty tree without `--additional`, a held claim) - shown, never blocking. `Spawn`: `os.Executable` (the chainFinish rule, not "pm" on PATH), `Setsid`, the log truncated for a fresh run and APPENDED when one is live, the run-state seeded so the queue shows the run at once, env **`PM_LAUNCH_SOURCE=cockpit`** which `pm run-epic`/`pm work`/`pm finish` stamp into their journal start line as **`source`** (`storage.LaunchSource`; a kill from the web stamps it on its `killed` line too), and `pm executor stats` prints "launched from: N cockpit, M cli/board". `Claim` = `storage.AcquireFinishClaim` with session **`cockpit-<host>`** and a heartbeat goroutine (`ClaimHeartbeat` 3 min, stops at `ClaimMaxHold` 1 h so a forgotten claim lapses on its TTL); a claim held by anyone else is a `*runctl.BusyError` (409, the body names host/pid/session/age); `Release` drops only a claim carrying this session on this host (after a restart too). The attention row of a live claim the cockpit itself holds (`storage.CockpitOwnsClaim`: host + the `cockpit-` prefix) carries the new **`release_claim`** action and says so in its reason. `Kill` is the board's `killRun` step for step (`pickRun` = liveness first, the run wins a tie; re-read by kind; `RunState.Kill(SIGTERM)` with a `*storage.StaleRunError` reported as 409 and NOTHING signalled; the failed stamp, the journal line with the subs so far, `ReleaseWorktreeLock`, the killed acceptance's own claim released by SESSION match, the in-flight sub parked on `waiting`, SIGKILL after `KillEscalation` 2 s only when the re-read state still names the pid); nothing to kill is a 409 too (`*NoRunError`). Errors: 404 unknown project/task, 400 unknown action / a body that is not `runctl.Flags`, 409 busy/stale/no-run, else 500; every POST needs the client header. `Options.RunControl` injects a controller whose `Exe` is a fake script in tests. **Web**: the run kinds are `PendingKind`s (`lib/confirmText` `isRunKind`); `useConfirmedAction` fetches `useRunPlan` (key `['run-plan', p, id, action, yolo, additional]`, `staleTime: 0`) while the dialog is open and re-fetches when a checkbox changes, the confirm button waits for the plan (`loading`), `flagOptions` says which checkboxes each action offers (resume_run: yolo + additional; rerun_finish: additional; the rest none) and disables `additional` when the project has no slots, `previewOf` renders `cd <cwd> && pm ...` (or the kill/claim target) in the dialog's `command preview` block, the plan's warnings render as alerts, and the toast carries pid + log / claim state / the parked sub (`runToast`). The `run` mutation kind invalidates `['runs']`, `['attention']`, `['tasks', p]`, `['context', p]`. `sleep_project` on a stuck-project row goes through the same `sleep_project` dialog as the settings screen. Tests: `internal/runctl/runctl_test.go` (plans incl. never-sim, spawn through a fake `pm` that records argv + `PM_LAUNCH_SOURCE`, the seeded state, kill + escalation + journal + park, stale and no-run, claim heartbeat / re-claim = refresh / release / foreign claim busy), `server_test.go` `TestRunControl` (plan 400/404, 403 without the header, spawn -> `/api/runs` live -> kill -> second kill 409, claim -> `release_claim` on the row -> release, foreign claim 409 naming the holder), `web` `confirmText.test.ts` "run actions" + `shell.test.tsx` "run control" (the plan preview, a flag re-plans, Esc posts nothing, confirm posts the flags, kill names the pid).
- **The LLM report over the feed (pm-cli-118-20) - `internal/report` + `/api/report`**: layer 2 over the change feed - one prose report per cutoff period, written by a headless `claude -p` from the RAW feed (the cache since the cutoff, `feed.Read`), the attention queue (rows, never bodies; the `changes` section is skipped - it IS the feed) and the group list, plus up to five suggestions as `- SUGGEST <task_id> <action>: <sentence>` lines with `<action>` from `report.Actions` (the cockpit's own mutation kinds: focus_toggle, set_waiting_for, back_to_todo, claim, rerun_finish, resume_run, sleep_project - nothing new; an unknown action or an unplaceable task id leaves the suggestion dismiss-only). **Off by default** (`cockpit.sources.report`) because it costs tokens; `cockpit.report {model: haiku, language: pl}` (`storage.ReportConfig`, validated non-empty one-word model; on `/api/config`, the settings PATCH and the settings screen's Sources group; `pm config show` prints it). **The hard rule, proven by `TestReport`: while the switch is off `pm serve` never starts claude** - `POST /api/report` is a 409 and a feed refresh writes nothing (SPEC-CONFLICT recorded on the sub: the Spec's "on demand always" yields to the AC; "always" means whenever enabled, regardless of the once-per-period rule). When on: written ONCE per period on its own after the first CLEAN feed refresh of that period - every ENABLED source ran without error (`sourcesClean` in `runRefresh`; `Feed.Refresh` returns nil for a source failure, so a 07:00 tick with gh unauthenticated or the laptop offline is NOT the period's refresh and the next clean one writes it; a stored failure of claude itself counts as written, so a broken claude is not retried every tick), or now on `POST /api/report` (202 `{state: writing}`, 409 while one is in flight; this path runs `runRefresh` FIRST inside the background write, because the cache can be a tick old - the PR #1002 report of 2026-09-14 called a merged PR ongoing off a 14:00 cache at 15:07); **the prompt carries CURRENT STATE beside the events** (`Input.PRs` = `Feed.PRStates(cutoff)`, `Input.Tasks` = the status + waiting_for of every task an event or a PR names, read at write time) and the instructions tell the model to describe each PR/task by that state and never a merged/closed PR or a done task as in progress; the write runs in the background and ends in **SSE `event: report`** (`{period}`), the client refetches `['report']`. `GET /api/report` = `{enabled, period, cutoff, state: off|none|writing|done|error, model, report?}`; `POST /api/report/dismiss {id}` hides a suggestion (stored in the report's `dismissed`, 400 on an unknown id). The run (`report.Writer.Run`) is the executor's contract restated in its smallest form because `internal/cmd` is out of reach (it imports the server): `claude -p <prompt> --output-format json --model <m> --max-turns 1 --tools "" --strict-mcp-config`, the worker's environment rule (`report.Environ`: ANTHROPIC_API_KEY/AUTH_TOKEN stripped so the subscription pays, `PM_HEADLESS=1`), its own process group SIGKILLed on `report.Timeout` (3 min), the CLI's two output shapes (object, or array whose `result` message is the last), `is_error` envelopes reported with their subtype, tokens off `usage` into `storage.TokenUsage`. pm writes the result itself (`report.Store` under `<pm-root>/.cockpit/reports/<period>.json` + `.md`, atomically; `PeriodKey` = the cutoff in the local zone, `2006-01-02T15`): a headless claude cannot write under `~/.claude`. Tests: `internal/report/report_test.go` (prompt pinned, suggestion parsing incl. unknown action, success/failure stored, both output shapes, the timeout kills the group, the env rule), `server_test.go` `TestReport` (off: 409 + refresh + zero calls of a fake claude that counts its invocations; on: one write per period with text/suggestions/tokens on GET and the prompt carrying the events + language + model; POST writes again and stores a failing claude as `error`; dismiss). **Web**: `useReport` (key `['report']`), `lib/reportView` (`reportView` words the five states, `openSuggestions` minus the dismissed, `costLine` "haiku · 4.4k tokens · 9 s", `paragraphsOf`, `canDo` = a known action on a placed task), `components/ReportPanel` (status line, prose paragraphs, the cost in small print, suggestions with `do` = the mapped action's ordinary dialog on that task and `dismiss` = a direct POST, `write now`/`write again` through the dialog as `write_report` because it spends tokens); the `report_write`/`report_dismiss` mutation kinds invalidate `['report']`; the SSE `report` event too. Tests: `lib/reportView.test.ts`, `shell.test.tsx` "report panel" (off without a button; done with prose, cost, an enabled and a disabled `do`; `do` -> the back_to_todo dialog -> the task POST; dismiss posts the id; write again asks then posts `/api/report`).
- **The slack source (pm-cli-118-19) - `internal/feed/source_slack.go` + `mcp_client.go`**: the fourth feed source, `feed.Configurable` off **`cockpit.slack`** (`storage.SlackSourceConfig`: `servers[]` = one MCP server per workspace label - `{workspace, claude_server: <name in ~/.claude.json's mcpServers, top-level or under any project>, me}` or `{workspace, command, args, env, me}`; `claude_config` overrides the file; validated: slug workspaces, unique, claude_server or command required; the switch is `cockpit.sources.slack`, OFF by default; `pm config show` lists the servers and `/api/config` carries `slack.workspaces` without secrets). Per refresh, per workspace: `ResolveServer` -> `DialStdio` (its own process group, SIGKILLed as a group; the handshake and every call under `SlackTimeout` 90 s) -> `conversations_history{channel_id: "#name", limit: "<N>d"}` for every channel the projects map (project.yaml `slack: {workspace, channels}`, 118-18; a channel mapped by two projects is read once, the first wins) -> when `me` is set, `conversations_search_messages{search_query: "@me" | "<@U..>", filter_date_after}` for mentions and `{filter_users_with: me, filter_date_after}` for DMs and threads with the user - ALWAYS, project-less (the feed's "no project" rows; a channel the search hits that a project maps is placed) - then the session closes. Events: `#channel · user: first line` (140 runes), `Detail` "workspace <ws>" (+ "mention · " / "dm · ", "· in a thread"), `URL` = the permalink column when the server gives one, severity info for a channel message and **warn** for a mention or a DM, the user's own messages skipped everywhere (not news to the user), ids stable over workspace+channel+ts+user+text so a refresh dedups. **The CSV parse is header-driven** (`parseMessages`: user/text/time/channel/thread/permalink columns matched by a few spellings each; the cursor row the server appends is dropped; `normalizeTS` reads a Slack ts, an epoch or any stamp) because slack-mcp-server's README does not pin its columns - a positional read would misfile a renamed column as text. Errors are PER WORKSPACE (`*ProjectError{Project: "workspace <ws>"}`, joined): a hung `npx`, a dead server, a missing `claude_server` entry, a project mapping a workspace with no server, and "no `me` configured - mentions and DMs skipped" - the other workspaces still run and the feed's other sources are untouched (the 118-14 contract). No servers at all is the source's error. Tests (`source_slack_test.go`) run the REAL subprocess path against **`testdata/fake_mcp.py`**, a stdio JSON-RPC server put on PATH as `slack-mcp-fake` (the fakeClaude pattern; canned CSV per tool+channel from a script file passed through the server spec's env; `FAKE_MCP_MODE=hang|die`): two workspaces, channel -> project mapping, a mention and a DM without a project, own messages and pre-cutoff rows dropped, the timeout biting per workspace while the other answers, a dying server, a mapped workspace with no server; `ResolveServer` over a fake claude.json (top-level, under a project, http type refused, unknown name); the header-driven parse. **No real Slack anywhere in tests**; the check against the user's workspaces is the acceptance session's, by hand, after `cockpit.slack.servers` is filled in (the user's servers are `slack-orbit` and `slack-vega` under their projects in `~/.claude.json`).
- **Dismiss, solo shifts, executor off (pm-cli-125/136/137/138)**: rows of `storage.DismissableSections` (needs_me, solo_reports, landed_no_pr) carry `dismiss`; `POST /api/attention/dismiss {rows:[{section,project,task_id|shift,since}]}` records them in `<pm-root>/.cockpit/dismissed.json` (`storage/dismissed.go`, key = section|project|id|since, so a NEW condition on the same task - another stamp - comes back by itself; never a task mutation), `BuildAttention` drops them and counts `dismissed` per section (Scope recounts), `POST /api/attention/restore {section[,project]}` brings them back. The web dismisses WITHOUT a dialog and offers "dismiss older than 14d (N)" + "N hidden · restore" per section (Today, group page). **Solo**: `storage/solo.go` parses the `/solo` skill's `<projectDir>/.shift/<date>-<id>.md` leniently (header kind, `## Status: closed <stamp>` in the spellings the skill wrote, `## Queue` lines `id · title · status` = the status at shift START, `## Progress*` lines give each task's `outcome` done/parked/untouched/doing and `branch` off `branch@sha`) + `-report.md`, which `storage/solo_report.go` `ParseShiftReport` splits along the skill's own skeleton (`prompts/report.md`: "Co z taskami" task blocks with Bug/Stan/Sprawdzone/Przed PR-em in the three spellings the skill has written, decisions, ideas, cleanup, technical, and the TL;DR split at the sentence addressed to the user - "Od Ciebie:", "Twój ruch:", "Do rana:" - into `summary` + `next`); every shift carries a `summary` (title = the report's own unless generic, else the first task heading "+N more"; outcome counts off the report's verdicts, else off Progress; `next` cut to 180 runes). Section `solo_reports` (on by default) = shifts closed in the last `SoloReportDays` 14 with a report, title = the summary's, reason = "6 done · 1 untouched · Od Ciebie: ...", severity warn when a task came back partial/not done/parked, actions `open_report` + dismiss (= read); `GET /api/solo` (every shift, open first) and `GET /api/solo/{project}/{shift}/report` (the id is matched against parsed shifts, never joined into a path; `kind: state` when no report yet; `digest` = the parsed report). Web `/solo/$project/$shift` is a digest, not the markdown (`lib/soloReportView` `digestView`, `components/SoloDigest`): a "your move" box (the TL;DR's `next`) + the summary, outcome chips, one line per task (glyph, heading, outcome, the Stan's lead with the verdict word dropped, the before-the-PR line with its point count) opening to State/Before the PR/Problem/How checked, then folds for decisions, ideas, cleanup, technical and the whole report; a report with no parsed tasks, or a state file, renders whole. "mark read" stays; the Runs Solo table (shown first) carries the summary title and the outcome counts. **`cockpit.show_executor`** (default false, settings checkbox, `/api/config`, PATCH): off = needs_me/landed_no_pr/in_progress emit no rows (note `storage.NoteExecutorHidden`, skipped in pm_context's digest notes), the sidebar's failed/visual counters ignore run-states, and the Runs screen hides the executor table. The report panel's write now/again posts directly (it already says it costs tokens).
- **PR code review from the web (`internal/review` + `/api/reviews`)**: `/review` screen takes a PR URL, a Slack message link or a sentence ("pr 555 w repo orbit mobile"); `Controller.Resolve` (`internal/review/resolve.go`) turns it into ONE PR cheapest-first: a GitHub PR URL anywhere in the text wins; a Slack permalink is read by pm itself through every `slack*` stdio server in `~/.claude.json` in parallel (`feed.ClaudeServerNames` + `ResolveServer` + `DialStdio`, tool `conversations_replies` on the permalink's channel + `thread_ts`, first answer wins - nothing records which server is which workspace) and a PR URL in the message wins; otherwise a one-turn tool-less haiku (`report.Writer.Run`) maps the text to a listed checkout's owner/repo + number as a JSON line, validated against the candidates; the result is confirmed with `gh pr view` (the project's `gh_account` token) and its title stored, all under `ResolveTimeout` 2 min inside the POST (a failure at any step = 400 naming it). `Controller.AskModel`/`ReadSlack`/`ViewPR` replace the three outside calls in tests. Then `Controller.Match` picks the checkout - an active project whose `repo` names that owner/repo, else one whose `git remote -v` does (an InputError = 400 naming the `repo:` fix when none) - and `Start` spawns `claude -p "/review <url>" --output-format json --permission-mode dontAsk --allowedTools <read-only list> --disallowedTools <every write>` DETACHED (`Setsid`) in a THROWAWAY DETACHED WORKTREE of the project's `path` (`prepareWorktree`: `$TMPDIR/pm-review/<id>` at `refs/pull/N/head` fetched from origin, else the checkout's HEAD, labelled in `commit`; removed when the process ends, or on the next read after a `pm serve` restart), env = `report.Environ()` with `CLAUDE_CONFIG_DIR` from the project's `claude_config_dir` and `GH_TOKEN` from `gh_account`. **Never the user's checkout, and the deny list is load-bearing**: `--allowedTools` only ADDS to the account's settings, and `~/.claude-alt/settings.json` allows git checkout/reset/stash - the first live review (2026-09-14) had a reviewer sub-agent run `git checkout origin/<branch> -- .` in the user's checkout and lose an uncommitted `ios/Podfile.lock`. Deny rules beat allow rules. **Approve** (`internal/review/approve.go`, `POST /api/reviews/{id}/approve`): ONE click on a done review's detail page (no confirmation step - the user's call; the question is the button's tooltip, "approve anyway" + the warning when the report found issues) makes pm run `gh pr review <n> --repo <r> --approve` (no body, no comment; the project's `gh_account` token) and, when the input was a Slack link, reacts `white_check_mark` on THAT message through the server that read it (`SlackLink.Server`, stored on the review) with `SLACK_MCP_REACTION_TOOL=<channel>` set for pm's own subprocess only - slack-mcp-server keeps `reactions_add` off by default and the user's config is never changed. Outcome in `<id>.approved|.approve_error|.reacted|.react_error`. **Start** reacts too: a request from a Slack link gets `eyes` (👀) on that message right after the spawn, in a background goroutine with its own context (`reactStarted` - the POST does not wait for the Slack server), outcome in `<id>.seen|.seen_error` (`slack_seen`/`slack_seen_error`, the detail page's header in any state via `lib/reviewView` `slackSeenLine`); a failed 👀 never fails the review and a PR URL input never reacts; approving twice is a no-op, a failed reaction is retried by the next approve (`already_reacted` counts as done). `no_issues` = a report line starting "No issues found" or "No issues survived" (the skill writes both); otherwise the button reads "approve anyway" with a warning. State lives in files only (`<pm-root>/.cockpit/reviews/<id>.json|.out|.err|.timeout|.cancelled`), so a review survives a `pm serve` restart: a live pid = running, a decodable `.out` = done (report + tokens via `report.Decode`), else error with the reason. `Timeout` 45 min kills the group; `POST /api/reviews/{id}/cancel` SIGTERMs it. Endpoints: `GET /api/reviews` (no report), `GET /api/reviews/{id}`, `POST /api/reviews {url}` (202), cancel; POSTs need the client header. Web: `routes/ReviewPage.tsx` (form + table, key `4`), `routes/ReviewDetailPage.tsx` (markdown report), `lib/reviewView`, queries poll every 5 s while a review runs. Tests: `internal/review/review_test.go` (fake claude records cwd/config dir/argv; remote match; fail/cancel), `server/reviews_test.go`, `shell.test.tsx` "review screen".
- **Solo launch from the web (pm-cli-141, `internal/solo` + `/api/solo`)**: the Runs screen's "launch solo" form starts an unattended `/solo` session as a **`claude --bg` background session** (CC 2.1.272 agent view; `claude attach <id>` jumps in from a terminal, Ctrl+Z detaches, `claude logs`/`claude stop`/`claude rm`), never headless `-p` (no way to jump in) and never tmux. `Controller.Plan(Input)` is the one place the argv is decided and exactly what `Start` runs, so the dialog's preview IS the command: the `cc-solo` rule restated once (`buildArgv`: `--dangerously-skip-permissions --autocompact 400k --settings '{"worktree":{"bgIsolation":"none"}}' [--setting-sources user,local iff the repo's .claude/settings.json has permissions.ask rules] [--model m] --name solo-<slug> --bg "/solo <queue> [--sim|--web|--no-runtime] [--push] [--pr] [--max-tasks N] [--max-hours H] [--base b]"`). **`bgIsolation: none` is load-bearing**: without it a bg session refuses Edit/Write in the main checkout until it moves into a `.claude/worktrees/` worktree, and solo works in the main checkout. `--session-id` is IGNORED with `--bg`, so the join is by the short id `--bg` prints (`backgrounded · <id> · <name>`, regex over stdout) and the session UUID (= the shift id) comes later from `claude agents --json --all` (the ONLY supported state interface; `~/.claude/jobs/<id>/state.json` is not a contract) - `Launch.state` is that row's `state` (`working|blocked|done|failed|stopped`; no row = `unknown`, or `starting` within `StartingGrace` 2 min; a stored `Stopped` stamp = `stopped`; a failed spawn = `error`), cached `AgentsTTL` 3 s because the web polls every 5 s while a launch is live (`.shift/` is not in the SSE fingerprint). The supervisor is PER CONFIG DIR, so every call (`--bg`, `agents`, `stop`) runs under the project's `claude_config_dir` and the table's attach command carries a `CLAUDE_CONFIG_DIR=<dir> ` prefix when it is not the default. **Launch env** (`solo.Environ`): `os.Environ()` minus `ANTHROPIC_API_KEY`/`AUTH_TOKEN` (the subscription pays), `CLAUDECODE`/`CLAUDE_PID`/every `CLAUDE_CODE_*`/`CLAUDE_JOB_DIR` (a `pm serve` started inside a CC session leaks its `CLAUDE_CODE_SESSION_ID`, and `pm session-id` reads that FIRST - the solo guard marker would carry a foreign id), plus `CLAUDE_CONFIG_DIR=<project's>` and `PM_LAUNCH_SOURCE=cockpit`; NEVER `PM_HEADLESS` (project hooks read it as "no simulator"). Stop is ONLY `claude stop <id>` - the supervisor RESTARTS a process killed by a signal. Records live in `<pm-root>/.cockpit/solo/<id>.json` + `.log` (the launcher's stdout/stderr; a failed start is `failed-<stamp>` with `error`), so a launch survives a `pm serve` restart. Warnings never block (the skill's Step 0 is the gate): claude not on PATH, ask rules without a `settings.local.json` deny mirror, dirty tree, base missing / origin ahead, an open `.shift/`, a live launch of the same project. Under the supervisor the claude process argv is `claude bg-spare --bg-spare <sock>`, so the skill's `launch-check.sh`/`window.sh` (in `~/.claude/skills/solo/scripts/`) read the flags from `$CLAUDE_JOB_DIR/state.json` `respawnFlags` first and walk the process tree only without one. Endpoints: `GET /api/solo` = `{shifts, launches}`, `GET /api/solo/plan?project=&queue=&runtime=&base=&model=&push=1&pr=1&max_tasks=&max_hours=`, `POST /api/solo` (body = `solo.Input`, 202), `GET /api/solo/{id}`, `POST /api/solo/{id}/stop`; InputError 400, unknown id/project 404, a failed spawn 502; POSTs need the client header. **Web**: `SoloLaunchForm` submits into the ONE dialog as `solo_start` (`lib/confirmText` `previewOfSolo` = `cd <cwd> && [CLAUDE_CONFIG_DIR=..] claude <argv shell-quoted>`, the plan's warnings as alerts, confirm waits for the plan; `useSoloPlan`, key `['solo-plan', <query string>]`), `lib/soloView` joins launches to shifts by `launch.session_id === shift.id` per project and gives a launch with no shift yet a row of its own (`hasFile: false`, no report link), `SoloTable` shows the state, the attach command with a copy button and a `stop` that goes through the dialog as `solo_stop`; the group page's Runs tab preselects the group's first repo. Tests: `internal/solo/solo_test.go` (fake `claude` script: records cwd/env/argv, prints the documented `--bg` block, serves canned `agents --json`, records `stop`; env rule, warnings on a temp repo, states, TTL, stop, records surviving a new controller, failures), `server/solo_test.go` `TestSolo`, `web` `soloView.test.ts`, `confirmText.test.ts` "solo actions", `shell.test.tsx` "solo launch".
- **The shell (pm-cli-118-9)**: `/p/$slug` is a LAYOUT route whose Outlet holds the task detail (`/p/$slug/t/$id`) - beside the list from 768 px up, instead of it below; `/runs` is the `pm runs` table with cells worded by `lib/runCells` (a port of `RunCell.String`/`AcceptCell.String` - keep them in step) and an explicit "fetch remote" button on `useRemoteRuns` (key `['runs-remote']`, deliberately NOT under `['runs']`, so a `runs` event can never trigger an ssh round-trip). The Spec/Log split is `lib/splitSpecLog` (the TUI's `bodyToDisplayMarkdown` rule), relative times are `lib/relativeTime` (calendar days in the reader's zone like the TUI, minutes/hours within the day; an empty `status_changed` renders as unknown, never as "just now"), palette matching is `lib/paletteItems` (cmdk's own filter is off), the key map is `lib/shortcuts` (one table drives the listener AND the `?` dialog). `api/useLiveInvalidation` is the one EventSource: a `tasks` event invalidates `['tasks', slug]`, `['task', slug]`, `['context', slug]`, `['projects']` (sidebar counts), `['groups']` and `['attention']`, a `runs` event `['runs']` + `['attention']`, a `changes` event `['changes']` + `['attention']`; `useChanges`/`useRefreshChanges`/`useMarkChangesSeen` (`apiPost` in `client.ts`) are the feed's hooks; reconnects are EventSource's own. PWA = `public/manifest.webmanifest` + generated `icon-192/512.png` (no service worker); the server registers the `.webmanifest` MIME type because Go's table lacks it. Tests: `src/lib/*.test.ts` per function, `src/shell.test.tsx` end to end over a fake `fetch` (detail zones, j/k/Enter/Esc incl. "j while typing does nothing", `?`, ⌘K filter + navigate, runs + fetch remote), `api/useLiveInvalidation.test.tsx` over a fake EventSource. jsdom shims live in `src/test/setup.ts` (`<dialog>` open/close, ResizeObserver for cmdk, scrollIntoView).
- **The look (pm-cli-118-10) - an editorial ledger, and the layer boundary held**: the visual pass replaced `components/`, `routes/` and `hooks/` and touched NOTHING in `web/src/api/` or `web/src/lib/` (`git diff --stat` against the epic: zero lines there), with the 140 vitest tests passing unchanged - the proof that the view layer is replaceable. Direction (chosen with the `frontend-design` skill, recorded in the task's Log): a morning paper, not a dashboard - masthead with the date (`.masthead`), section heads in a serif (`SectionHead`: Fraunces title + tabular count + the "why" line in the margin), rows as dispatch lines separated by hairlines (`.ledger-row`, the keyboard selection = a highlighter band via `aria-current`), no cards, no icons over headings. Tokens live in `web/src/index.css` as OKLCH custom properties (`--paper/--ink/--rule` + `--crit/--warn/--info/--ok` with `-bg` pairs) mapped onto shadcn's semantic names in `@theme inline`; the dark theme is the `.dark` class on `<html>` (`hooks/useTheme`: system by default, light/dark by the sidebar-footer toggle, remembered in localStorage - UI state, never a setting), lighter surfaces instead of shadows and weight 380 instead of 400. Fonts ship in the bundle (`@fontsource-variable/fraunces` wght axis + `@fontsource-variable/instrument-sans`, ~110 KB of woff2 in `dist/assets`) so the binary works offline and over Tailscale; mono is the system stack, used only where alignment matters (`.id`, `.num`). Material: shadcn/ui via `components.json` with the `@/` alias (tsconfig `paths` + vite `resolve.alias`) and `cn` in `components/ui/cn.ts` (NOT `lib/`), only the copied components `button badge input textarea separator kbd table tooltip`; lucide icons 12-16 px next to a text label, never instead of one (the label is the accessible name the tests select by). Deliberately NOT Radix Dialog/Select/Checkbox/Tabs: the tests assert a native `<dialog open>`, `selectOptions` on a combobox, checkbox roles and tabs as URL links, and swapping those would change behaviour, not looks. Utility classes for the recurring pieces (`.chip`, `.pill`, `.ghost-btn`, `.field`, `.kicker`, `.ledger-table`, `.sev-*`) live in `@layer components` so a component file reads as structure. Motion: the home sections rise in once (`.rise`, 40 ms stagger capped at 8), dialogs 180 ms, the toast from below; `prefers-reduced-motion` collapses all three to a fade. Phone (390 px): the sidebar becomes a sticky top bar (masthead, the four screens, the group chips as a horizontal strip), the row hides its project chip into the reason line, the action buttons wrap under the title with coarse-pointer padding. Known: Chrome's window will not resize under ~500 px, so the phone check runs in Playwright at a 390 px viewport; the vite bundle is 634 KB minified (197 KB gzip) in one chunk - code-splitting is a later polish.
- **Timeline in the cockpit**: `GET /api/timeline/{project}` -> `service.TimelineDefaultRead` (the no-filter `pm_timeline_list` shape, 404 on an unknown project, GET only - entries are written over MCP and the CLI). The SSE poll folds `.timeline/*.jsonl` into the `tasks` fingerprint, so a timeline write is a `tasks` event and `useLiveInvalidation` invalidates `['timeline', slug]` with it. Web: `TimelineRead`/`TimelineEntry` in `types.ts`, `timelineQuery`/`useTimeline` (key `['timeline', slug]`), `lib/timelineView` `leftOffView` decides per member repo - a timeline with entries shows the state and its date, the entries after it (date, kind, URL refs as links) and `staleLine`; no read or an empty timeline falls back to the repo's `notes`; the notes edit action stays on every repo (the only place the web edits notes). `components/LeftOff` takes `rows` of `{project, view}`. Tests: `server/timeline_test.go` (`TestTimeline`; `TestEventsTimeline` with a stray-file negative control), `lib/timelineView.test.ts`, `useLiveInvalidation.test.tsx`, `shell.test.tsx` "group page".

## Journals (`pm journal`) — per-project record of a flaky subsystem

A **journal** is a per-project append-only record of how ONE declared, repeatedly-troublesome subsystem actually behaves (0.35.0, pm-cli-83). Which subsystems a project journals is declared in `project.yaml` `journals:` (name + one-line subject); an undeclared name is an error, never an implicit create. (history: docs/design-log.md)

- **RULE vs EVENT**: a memory file or doc holds the RULE (current truth, rewritten); a journal entry holds an EVENT (dated, append-only, never rewritten); `fix` NAMES the rule it produced. Empty `fix` = OPEN, and the open set IS the backlog (derived, never hand-maintained). (history: docs/design-log.md)
- **Storage** (`internal/storage/journal.go`): JSONL per subject at `<projectDir>/.journal/<name>.jsonl`; `Incident` = `{ts, symptom, false_conclusion, cause, cost_min, fix, tags, session}`; `AppendIncident` RETURNS its write error. (history: docs/design-log.md)
- **Agnostic by config**: `Project.Journals` is the whole of the mechanism's project-awareness - nothing in the code knows what a simulator is. A journal NAME is a file name (`ValidateJournalName` = the `ValidateSlug` rule); a typo'd name would silently open a second file and split one history in half - hence undeclared-is-an-error in both CLI and MCP.
- **Closing an entry is a new EVENT, never an edit** (0.36.0): `Incident.Resolves []string` closes earlier entries (`StillOpen` = no own `fix` AND not resolved later); an unknown `resolves` id is REJECTED before the write; ids are DERIVED (`incidentID`), so pre-existing lines are closable. (history: docs/design-log.md)
- **Ordering**: entries read newest-first; an entry whose `ts` does not parse sorts LAST; `JournalStats.First/Last` come off the PARSED instant. (history: docs/design-log.md)
- **The cost average divides by `Costed`, not `Total`** - entries that recorded a cost. Dividing by the total would make a subsystem look CHEAPER the more often someone skipped the field, i.e. exactly backwards.
- **Surfaces**: `pm journal list|show|add|stats` (CLI, `--project` flag) and `pm_journal_add`/`pm_journal_list` (MCP); add reports back the recurrence its entry produced; stats travel WITH the entries. (history: docs/design-log.md)
- **`pm_context` / `pm context` get COUNTS ONLY**, never content (same rule as the executor profile pointer, 0.28.1): a journal grows without bound and its readers are the sessions about to touch that subsystem, not every session. No journals declared = no key at all.

## Executor (`pm work` / `pm run-epic` / `pm finish`)

Runs pm tasks/epics via isolated headless `claude -p` workers (implement -> test -> review -> verify). **The full internals live in `docs/executor.md`** (worktree slots, review policy, run-state/journal/heartbeat, kill + crash forensics, acceptance claims, baseline, independent mode, telemetry) - read it before touching `internal/cmd/work*.go`, `run_epic*.go`, `finish*.go`, `worker_guard.go` or `internal/storage/executor*.go`. The rules that bite regardless of what you are working on:

- **Headless workers cannot write under `~/.claude`** (CC sensitive-path guard, a hard refusal with nobody to approve) - source lives in `~/repos/pm-cli`, data in `~/.claude/pm/`.
- **Review is decided by pm, not the worker** (0.38.0): reviewer count, model (`executor.review_model`, default opus), packet (the whole diff) and rounds (`executor.fix_rounds`) are enforced by the `pm worker-guard` PreToolUse hook, which also blocks hook bypasses and `.git/` writes - in `--yolo` too. Since pm-cli-130 there is a FLOOR as well: a `verified` on a non-empty production diff with zero reviewer spawns that ran is refused at `StructuredOutput` time and, if it still arrives, demoted to `blocked` + `REVIEW-SKIPPED:` by `demoteUnreviewed` (`review_floor.go`) before anything merges or pushes.
- **Test executor paths through the fake `claude` binary** (`fakeClaude` in `internal/cmd/work_exec_test.go`), never by spending tokens; the envelope has two shapes (object, or a message array whose last element is the object - `decodeClaudeOutput`).
- **Every run journals itself** (`<project>/.executor/journal.jsonl`, append-only; `pm executor stats` is the rollup, `/executor-retro` the reader) - a new run-time fact belongs in the run-state + journal, not in a log line.
- **The web can start and stop runs too** (pm-cli-118-21): `pm serve`'s `POST /api/runs/{p}/{id}/{action}` goes through `internal/runctl`, which mirrors the board's launch/kill/claim rules (argv preview = the argv that runs, never `--sim`, `PM_LAUNCH_SOURCE=cockpit` -> journal `source`, a claim session `cockpit-<host>` with a 3-min heartbeat for at most 1 h). A change to how the board launches or kills has a twin in `runctl` to keep in step.
- **Tokens, not dollars, are what the subscription window meters** (0.55.0): per-sub `tokens` off the envelope's `usage`; workers run under `executor.worker_claude_config_dir` when set (a slim config dir without the user-scope skill catalogue / CLAUDE.md), and the worker prompt carries a call discipline (no prose between tools, batch independent calls) - see pm-cli-119.

